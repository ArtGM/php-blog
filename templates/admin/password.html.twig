{% extends 'admin/dashboard.html.twig' %}

{% block admin_content %}
    <form action="/admin/password/update" method="post" id="update_password">
        <div class="fields">
            <div class="field">
                <label for="password">Mot de passe</label><input name="password" type="password" value=""
                                                                 placeholder="Nouveau mot de passe">
            </div>
        </div>
        <input id="user-id" type="hidden" name="id" value="{{ user.id }}">
        <ul class="actions">
            <li><input class="button" type="submit" value="Mettre à jour"></li>
            <li><a href="/admin/profile/{{ user.id }}-{{ user.username |lower }}">Retour au profil</a></li>
        </ul>
    </form>
{% endblock %}