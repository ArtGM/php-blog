{% block comment %}
    <h2>Commentaires :</h2>
<ul>
    {% for comment in comments  %}
    <li>
        <time>le {{ comment.createTime }}</time>
        <p>{{ comment.content }}</p>
        <p>Pseudo: <strong>{{ comment.userName }}</strong></p>
    </li>
    {% endfor %}
</ul>
{% endblock %}
